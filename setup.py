import setuptools

setuptools.setup(name='load_runner',
                 version='0.01',
                 description='Load test tool',
                 packages=setuptools.find_packages(),
                 include_package_data=True)
                 # setup_requires=['fabric'])
